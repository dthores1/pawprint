/// <reference types="vite/client" />

// Injected by Vite `define` (see vite.config.ts) — the package.json version,
// used to auto-attach the running app version to support tickets.
declare const __APP_VERSION__: string;
